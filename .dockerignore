# Build artifacts & deps — regenerated inside the image
node_modules
.next
out
build
coverage

# VCS & local tooling
.git
.gitignore
.claude
.vscode

# Secrets — never bake real env into an image (keep the template)
.env
.env.*
!.env.example

# Docker / CI meta
Dockerfile
.dockerignore
docker-compose*.yml

# Logs & OS cruft
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.DS_Store

# Local SQLite leftovers (pre-Postgres)
prisma/*.db
prisma/*.db-journal

# Docs not needed at build time
README.md
*.md
